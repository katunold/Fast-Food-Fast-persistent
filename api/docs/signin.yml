signin
---
tags:
- User Signup and Login
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This route is used to login a user to his accout.
responses:
  200:
    description: Successful login. JWT token generated (OK).
    schema:
      id: User Login
      properties:
        user_name:
          type: string
          default: arnold
        password:
          type: string
          default: password
  400:
    description: Your request is not appropriate (Bad Request).

